export default defineAppConfig({
  wiki: {
    mode: 'select', // 'tabs', or 'select'
    tabs: {
      count: 2,
      showIcons: true,
    },
  },
  foo: 'bar', // Keeping the existing configuration
})
